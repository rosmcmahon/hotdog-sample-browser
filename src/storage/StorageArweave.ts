import Arweave from 'arweave/web'

const arweave = Arweave.init({});
const appName = "hotdog-sample-browser-test1"
const appVersion = "0.0.1"
console.log("USING App-Name: "+ appName )
console.log("USING App-Version: "+ appVersion)

var uploading = false
